<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_emi_tenure_plan_list" model="ir.ui.view">
        <field name="name">emi.tenure.plan.list</field>
        <field name="model">emi.tenure.plan</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="months"/>
                <field name="name"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="view_emi_tenure_plan_search" model="ir.ui.view">
        <field name="name">emi.tenure.plan.search</field>
        <field name="model">emi.tenure.plan</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="action_emi_tenure_plan" model="ir.actions.act_window">
        <field name="name">Tenure Plans</field>
        <field name="res_model">emi.tenure.plan</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_emi_tenure_plan_search"/>
    </record>

</odoo>
